[project]
name = "gigaloom"
version = "0.5.1a2"
description = "GigaLoom local, provider-neutral control plane for coding agents"
authors = [{ name = "Ruslan Yakupov", email = "rusyak1999@gmail.com" }]
requires-python = ">=3.11,<3.15"
readme = "packages/gpt2giga-harness/README.md"
license = "MIT"
dependencies = [
    "anyio>=4.10,<5",
    "fastapi>=0.133.0,<1",
    "pydantic>=2.12.0,<3",
    "pyjwt[crypto]>=2.12.0,<3",
    "pyyaml>=6.0,<7",
    "python-dateutil>=2.9.0,<3",
    "starlette>=1.1,<2",
    "textual>=8.2.8,<9",
    "uvicorn>=0.41.0,<1",
]

[project.urls]
Homepage = "https://krakenalt.github.io/gigaloom/"
Repository = "https://github.com/krakenalt/gigaloom"
Documentation = "https://krakenalt.github.io/gigaloom/"
Issues = "https://github.com/krakenalt/gigaloom/issues"
Changelog = "https://github.com/krakenalt/gigaloom/blob/main/packages/gpt2giga-harness/CHANGELOG_en.md"

[project.scripts]
gpt2giga-harness = "gpt2giga_harness.entrypoint:main"
giga = "gpt2giga_harness.entrypoint:main"
giga-skills-catalog-proxy = "gpt2giga_harness.skills_catalog_proxy:main"

[project.entry-points."gpt2giga.harnesses"]
direct-chat = "gpt2giga_harness.harnesses.direct_chat:DirectChatHarness"
codex-cli = "gpt2giga_harness.harnesses.codex_cli:CodexCliHarness"
claude-code = "gpt2giga_harness.harnesses.claude_code:ClaudeCodeHarness"
gemini-cli = "gpt2giga_harness.harnesses.gemini_cli:GeminiCliHarness"
echo = "gpt2giga_harness.harnesses.echo:EchoHarness"

[project.entry-points."agent_workbench.harness_adapters.v1"]
direct-chat = "gpt2giga_harness.harnesses.direct_chat:DirectChatHarness"
codex-cli = "gpt2giga_harness.harnesses.codex_cli:CodexCliHarness"
claude-code = "gpt2giga_harness.harnesses.claude_code:ClaudeCodeHarness"
gemini-cli = "gpt2giga_harness.harnesses.gemini_cli:GeminiCliHarness"
echo = "gpt2giga_harness.harnesses.echo:EchoHarness"

[project.entry-points."agent_workbench.provider_adapters.v1"]
direct-chat-legacy = "gpt2giga_harness.provider_profiles:direct_chat_legacy_compatibility"
codex-legacy = "gpt2giga_harness.provider_profiles:codex_legacy_compatibility"
claude-legacy = "gpt2giga_harness.provider_profiles:claude_legacy_compatibility"
gemini-legacy = "gpt2giga_harness.provider_profiles:gemini_legacy_compatibility"

[project.entry-points."agent_workbench.environment_providers.v1"]
git = "gpt2giga_harness.environments:git_environment_provider_plugin"

[project.optional-dependencies]
gpt2giga = [
    "gpt2giga==0.2.6a1",
    "gigachat>=0.2.2a1,<0.3.0",
]
claude-sdk = [
    "claude-agent-sdk>=0.2.122,<0.3",
]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build]
artifacts = [
    "packages/gpt2giga-harness/src/gpt2giga_harness/evidence/**",
    "packages/gpt2giga-harness/src/gpt2giga_harness/integration_sdk_preview/**",
    "packages/gpt2giga-harness/src/gpt2giga_harness/ui/cockpit_v2/assets/**",
]

[tool.hatch.build.hooks.custom]
path = "packages/gpt2giga-harness/hatch_build.py"

[tool.hatch.build.targets.wheel]
packages = ["packages/gpt2giga-harness/src/gpt2giga_harness"]

[tool.hatch.build.targets.sdist]
only-include = [
    "packages/gpt2giga-harness/src",
    "packages/gpt2giga-harness/asset_contract.py",
    "packages/gpt2giga-harness/hatch_build.py",
    "packages/gpt2giga-harness/CHANGELOG.md",
    "packages/gpt2giga-harness/CHANGELOG_en.md",
]

[tool.coverage.run]
omit = [
  "tests/*",
  "scripts/*",
  "docs/*",
  "*/migrations/*",
  "examples/*",
  "*/__pycache__/*",
  ".local/"
]
[tool.coverage.report]
exclude_lines = [
    "pragma: no cover",
    "def __repr__",
    "if self.debug",
    "if settings.DEBUG",
    "raise AssertionError",
    "raise NotImplementedError",
    "if 0:",
    "if __name__ == .__main__.:",
    "class .*\\bProtocol\\):",
    "@(abc\\.)?abstractmethod"
]
[dependency-groups]
dev = [
    "black (>=26.3.1,<27.0.0)",
    "httpx2 (>=2.9.1,<3)",
    "pytest (>=9.0.3, <10.0.0)",
    "pytest-asyncio (>=1.2.0,<2.0.0)",
    "pytest-mock (>=3.15.1,<4.0.0)",
    "pytest-xdist (>=3.8.0,<4.0.0)",
    "ruff (>=0.15,<0.16)",
    "pytest-cov (>=7.0.0,<8.0.0)",
    "pre-commit (>=4.3.0,<5.0.0)",
]
integrations = [
    "openai-agents (>=0.17,<1)",
    "pyjwt (>=2.12.0)",
    "python-multipart (>=0.0.27, <0.1.0)"
]
