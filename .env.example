# ==============================================================================
# gpt2giga — пример конфигурации
# ==============================================================================
# Скопируйте этот файл в .env и укажите реальные значения.
# Подробный справочник по смыслу настроек: docs/configuration.md
#
#   cp .env.example .env
#
# Docker Compose:
# - сервисы в `deploy/base.yaml` включаются через профили `DEV`/`PROD`
# - запуск: `docker compose --env-file .env -f deploy/base.yaml --profile PROD up -d`
#   или `docker compose --env-file .env -f deploy/base.yaml --profile DEV up -d`
#
# ВАЖНО: Для production-среды обязательно:
#   - установите GPT2GIGA_MODE=PROD
#   - включите GPT2GIGA_ENABLE_API_KEY_AUTH=True и задайте надёжный API ключ
#   - установите GIGACHAT_VERIFY_SSL_CERTS=True
#   - ограничьте CORS origins вместо wildcard ["*"]
#
# ==============================================================================
# Чеклист по безопасной эксплуатации (production)
# ==============================================================================
# 1. GPT2GIGA_MODE=PROD                 — отключает /docs, /logs*, подробный вывод
# 2. GPT2GIGA_ENABLE_API_KEY_AUTH=True  — включает API-key на всех эндпоинтах
# 3. GPT2GIGA_API_KEY=<сильный секрет>  — минимум 32 случайных символа
# 4. GIGACHAT_VERIFY_SSL_CERTS=True     — проверяет TLS сертификаты до API GigaChat
# 5. GPT2GIGA_CORS_ALLOW_ORIGINS=["https://your-app.example.com"]
# 6. GPT2GIGA_LOG_REDACT_SENSITIVE=True (по умолчанию) — маскирует секреты в логах
# 7. GPT2GIGA_LOG_LEVEL=INFO            — избегайте DEBUG в production
# 8. GPT2GIGA_MAX_REQUEST_BODY_BYTES=10485760 — ограничение тела запроса (10 МБ)
# 9. Запускайте за reverse proxy (nginx/Caddy) с TLS termination
# ==============================================================================

# --- Настройки прокси ---------------------------------------------------------
GPT2GIGA_MODE=DEV
GPT2GIGA_HOST=0.0.0.0
GPT2GIGA_PORT=8090
GPT2GIGA_LOG_LEVEL=INFO
GPT2GIGA_ENABLE_API_KEY_AUTH=True
GPT2GIGA_API_KEY="<REPLACE_WITH_STRONG_SECRET>"
# Задайте, только если нужно принудительно добавлять max_tokens при отсутствии лимита в запросе.
# GPT2GIGA_DEFAULT_MAX_TOKENS=128000
# GPT2GIGA_ENABLE_IMAGES=True
# При pass-through авторизации переиспользовать не более 32 credential-specific клиентов.
# GPT2GIGA_PASS_TOKEN=False
# GPT2GIGA_PASS_TOKEN_CLIENT_CACHE_SIZE=32

# Включить reasoning по умолчанию (добавляет reasoning_effort="high" в запросы к GigaChat,
# если клиент не указал reasoning_effort явно).
# GPT2GIGA_ENABLE_REASONING=False

# Полностью отключить передачу reasoning в GigaChat: удаляет reasoning/reasoning_effort
# из payload, включая явные параметры клиента и extra_body passthrough.
# GPT2GIGA_DISABLE_REASONING=False

# Режим structured output:
# - function_call (по умолчанию) — совместимый fallback через function calling;
# - native — нативное response_format в GigaChat SDK 0.2.1+ (требует поддержки модели/API).
# GPT2GIGA_STRUCTURED_OUTPUT_MODE=function_call

# Backend API contract для chat-like запросов к GigaChat:
# - v1 (по умолчанию) — root compatibility methods achat/astream;
# - v2 — GigaChat chat-completion contract via achat.create/achat.stream.
# Root routes без versioned prefix используют это значение.
# Публичный prefix /v1 принудительно выбирает GigaChat v1 contract,
# публичный prefix /v2 — GigaChat v2 contract для конкретного запроса.
# GPT2GIGA_GIGACHAT_API_MODE=v1

# Отключить маппинг provider built-in tools в GigaChat v2 built-in tools.
# Пользовательские function tools при этом продолжают работать.
# GPT2GIGA_DISABLE_BUILTIN_TOOL_MAPPING=False

# --- Экспериментальные флаги modular roadmap --------------------------------
# По умолчанию эти флаги сохраняют текущий legacy runtime path.
# GPT2GIGA_NORMALIZATION_MODE=off  # off | shadow | on; on routes OpenAI Chat through the experimental normalized path
# GPT2GIGA_LEGACY_CHAT_FALLBACK=True
# GPT2GIGA_CONVERSATION_STITCHING_ENABLED=False
# GPT2GIGA_CONVERSATION_TTL_SECONDS=3600
# GPT2GIGA_CONVERSATION_MAX_MESSAGES=40
# GPT2GIGA_CONVERSATION_USE_SESSION_ID=False
# GPT2GIGA_CONVERSATION_ON_DIVERGENCE=client_wins  # client_wins | fork
# GPT2GIGA_TRAFFIC_LOG_ENABLED=False
# GPT2GIGA_TRAFFIC_LOG_SINK=noop
# GPT2GIGA_TRAFFIC_LOG_SINKS=postgres,opensearch
# GPT2GIGA_TRAFFIC_LOG_JSONL_PATH=traffic_logs.jsonl
# GPT2GIGA_TRAFFIC_LOG_POSTGRES_DSN=postgresql://user:password@localhost:5432/gpt2giga
# GPT2GIGA_POSTGRES_DB=gpt2giga
# GPT2GIGA_POSTGRES_USER=gpt2giga
# GPT2GIGA_POSTGRES_PASSWORD="<REPLACE_WITH_STRONG_POSTGRES_PASSWORD>"
# GPT2GIGA_POSTGRES_PORT=5432
# GPT2GIGA_OPENSEARCH_URL=http://localhost:9200
# GPT2GIGA_OPENSEARCH_USERNAME=
# GPT2GIGA_OPENSEARCH_PASSWORD=
# GPT2GIGA_OPENSEARCH_INDEX=gpt2giga-traffic
# GPT2GIGA_OPENSEARCH_DATA_STREAM=True
# GPT2GIGA_OPENSEARCH_BULK_SIZE=500
# GPT2GIGA_OPENSEARCH_FLUSH_INTERVAL_MS=2000
# GPT2GIGA_OPENSEARCH_PORT=9200
# GPT2GIGA_TRAFFIC_LOG_CAPTURE_CONTENT=False
# GPT2GIGA_TRAFFIC_LOG_QUEUE_SIZE=10000
# GPT2GIGA_TRAFFIC_LOG_BATCH_SIZE=500
# GPT2GIGA_TRAFFIC_LOG_FLUSH_INTERVAL_MS=2000
# GPT2GIGA_TRAFFIC_LOG_DROP_ON_BACKPRESSURE=True
# GPT2GIGA_TRAFFIC_LOG_REDACT_SENSITIVE=True
# GPT2GIGA_TRAFFIC_LOG_REDACT_EXTRA_KEYS=[]
# GPT2GIGA_TRAFFIC_LOG_RETENTION_DAYS=30
# GPT2GIGA_TRAFFIC_LOG_PURGE_INTERVAL_SECONDS=3600
# GPT2GIGA_OBSERVABILITY_ENABLED=False
# GPT2GIGA_OBSERVABILITY_BACKEND=phoenix
# Gateway-prefixed names are preferred. Unprefixed PHOENIX_* remain supported
# as compatibility fallbacks for Phoenix tooling.
# GPT2GIGA_PHOENIX_COLLECTOR_ENDPOINT=http://localhost:4317
# GPT2GIGA_PHOENIX_PROJECT_NAME=gpt2giga
# GPT2GIGA_PHOENIX_API_KEY=
# PHOENIX_COLLECTOR_ENDPOINT=http://localhost:4317
# PHOENIX_PROJECT_NAME=gpt2giga
# PHOENIX_API_KEY=
# PHOENIX_PORT=6006
# PHOENIX_GRPC_PORT=4317
# GPT2GIGA_OBSERVABILITY_SAMPLE_RATE=1.0
# GPT2GIGA_OBSERVABILITY_QUEUE_SIZE=10000
# GPT2GIGA_OBSERVABILITY_DROP_ON_BACKPRESSURE=True
# GPT2GIGA_OBSERVABILITY_CAPTURE_CONTENT=False
# GPT2GIGA_OBSERVABILITY_CAPTURE_MESSAGES=False
# GPT2GIGA_OBSERVABILITY_CAPTURE_TOOL_ARGS=False
# GPT2GIGA_OBSERVABILITY_CAPTURE_RESPONSES=False
# GPT2GIGA_OBSERVABILITY_MAX_CONTENT_LENGTH=8000
# GPT2GIGA_OBSERVABILITY_REDACTION_ENABLED=True
# GPT2GIGA_METRICS_ENABLED=False
# GPT2GIGA_METRICS_PATH=/metrics
# GPT2GIGA_UI_ENABLED=False
# GPT2GIGA_DEBUG_TRANSLATE_ENABLED=False
# GPT2GIGA_ADMIN_API_ENABLED=False
# GPT2GIGA_ADMIN_API_KEY="<REPLACE_WITH_STRONG_ADMIN_SECRET>"
# GPT2GIGA_REPLAY_ENABLED=False

# --- Unified Harness CLI/UI --------------------------------------------------
# Local browser UI/session history for `giga ui` and `giga session ...`.
# History is written outside the repo by default and redacted before persistence.
# GPT2GIGA_HARNESS_PROXY_URL=http://127.0.0.1:8090
# GPT2GIGA_HARNESS_API_KEY="<LOCAL_PROXY_API_KEY>"
# Shared only by Harness and an externally managed proxy for signed model pins.
# GPT2GIGA_HARNESS_MODEL_KEY="<REPLACE_WITH_STRONG_RANDOM_SECRET>"
# GPT2GIGA_HARNESS_DEFAULT_MODEL=GigaChat-2-Max
# GPT2GIGA_HARNESS_DEFAULT_API_MODE=v2
# GPT2GIGA_HARNESS_UI_HOST=127.0.0.1
# GPT2GIGA_HARNESS_UI_PORT=8091
# Required for authenticated remote UI access; keep it out of commands and URLs.
# GPT2GIGA_HARNESS_UI_BOOTSTRAP_TOKEN="<REPLACE_WITH_STRONG_RANDOM_SECRET>"
# Optional comma-separated DNS names accepted by Host validation.
# GPT2GIGA_HARNESS_UI_ALLOWED_HOSTS=harness.example.internal
# GPT2GIGA_HARNESS_AUTO_START_PROXY=True
# GPT2GIGA_HARNESS_PROXY_START_TIMEOUT_SECONDS=15
# GPT2GIGA_HARNESS_DATA_DIR=~/.gpt2giga/harness
# `giga worker` uses the same data dir but never auto-starts a proxy. Configure
# a running proxy and this local API key for Direct Chat / external agent jobs.
# Worker coordination, redacted job payloads, and bounded logs are stored under
# GPT2GIGA_HARNESS_DATA_DIR/runtime*.

# Per-model лимиты одновременных upstream model-call внутри одного процесса gpt2giga.
# Feature выключена, если словарь пустой и default не задан.
# Глобальный SDK/HTTP limit GIGACHAT_MAX_CONNECTIONS остаётся отдельной настройкой.
# GPT2GIGA_MODEL_MAX_CONNECTIONS='{"GigaChat":1,"GigaChat-Pro":1,"GigaChat-Max":5}'
# GPT2GIGA_MODEL_MAX_CONNECTIONS_DEFAULT=1
# GPT2GIGA_MODEL_MAX_CONNECTIONS_ACQUIRE_TIMEOUT=30

# Глобальный лимит размера HTTP-тела запроса (в байтах, по умолчанию 10 МБ).
# GPT2GIGA_MAX_REQUEST_BODY_BYTES=10485760

# CORS списки в формате JSON-массивов:
# В production ограничьте origins конкретными доменами.
# GPT2GIGA_CORS_ALLOW_ORIGINS=["https://your-app.example.com"]
# GPT2GIGA_CORS_ALLOW_METHODS=["GET","POST","OPTIONS"]
# GPT2GIGA_CORS_ALLOW_HEADERS=["authorization","content-type","x-api-key"]

# IP allowlist для /logs* (пусто = без ограничений, недоступно в PROD)
# GPT2GIGA_LOGS_IP_ALLOWLIST=["10.0.0.1"]
# Доверять X-Forwarded-For только от этих точных IP reverse proxy.
# GPT2GIGA_LOGS_TRUSTED_PROXIES=["127.0.0.1"]

# --- Настройки GigaChat -------------------------------------------------------
GIGACHAT_SCOPE=GIGACHAT_API_PERS
GIGACHAT_CREDENTIALS="<REPLACE_WITH_YOUR_GIGACHAT_CREDENTIALS>"
#GIGACHAT_USER=
#GIGACHAT_PASSWORD=
#GIGACHAT_BASE_URL=
GIGACHAT_PROFANITY_CHECK=False
GIGACHAT_MODEL=GigaChat-2-Max
GIGACHAT_VERIFY_SSL_CERTS=False
GIGACHAT_TIMEOUT=70000
# GIGACHAT_MAX_CONNECTIONS=7

# --- Live integration tests ---------------------------------------------------
# Храните реальные значения в локальном .env.live, не в этом файле.
# GPT2GIGA_RUN_LIVE_TESTS=1
# GPT2GIGA_LIVE_ENV_FILE=.env.live
# GPT2GIGA_LIVE_MODEL=GigaChat-2-Max
# GPT2GIGA_LIVE_EMBEDDINGS_MODEL=EmbeddingsGigaR
# GPT2GIGA_LIVE_BACKEND_MODES=v1,v2


# --- Настройки mitmproxy -------------------------------------------------------
#HTTPS_PROXY="http://mitmproxy:8080"
#HTTP_PROXY="http://mitmproxy:8080"
#MITMPROXY_PASSWORD=12345
#MITMPROXY_PORT=8080
#MITMPROXY_WEB_PORT=8081
